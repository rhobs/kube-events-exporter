linters:
  enable:
    - megacheck
    - govet
  disable:
    - maligned
    - prealloc
  disable-all: false
  presets:
    - bugs
    - unused
  fast: false

linters-settings:
  goimports:
    local-prefixes: github.com/rhobs/kube-events-exporter

issues:
  exclude-use-default: false
